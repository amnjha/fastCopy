package code.project.fastCopy.data;

public enum CopyStrategy {
    BUFFERED_READER,
    BUFFERED_STREAMS,
    CUSTOM_BUFFER_BUFFERED_READER,
    CUSTOM_BUFFER_BUFFERED_STREAMS,
    CUSTOM_BUFFER_READER,
    CUSTOM_BUFFER_STREAMS,
    NATIVE_COPY,
    NATIVE_READER,
    NATIVE_STREAMS,
    NIO_BUFFER,
    NIO_TRANSFER,
    FILES_PATH
}

