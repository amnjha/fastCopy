package code.project.fastCopy.data;

public enum DiskType {
    /**
     * Copy on a single hardware modules
     */
    SINGLE_DISK,
    /**
     * Copy between two different hardware modules
     */
    MULTI_DISK;
}
